{
  "name": "mkdocs-documentation",
  "version": "1.0.0",
  "description": "Material for MkDocs deployed on Vercel",
  "scripts": {
    "build": "pip install -r requirements.txt && mkdocs build"
  }
}
